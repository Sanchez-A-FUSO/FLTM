# Core stack + TensorFlow. tensorflow-federated is NOT listed: its metadata pins
# grpcio~=1.46.3, which has no usable cp311-win wheel on PyPI and often fails to build.
# Install TFF in a second step (see scripts/install_windows.ps1 or the lines at the bottom).
#
# If you see: WARNING: Ignoring invalid distribution ~ip
#   Remove broken directories under Python311\Lib\site-packages whose names start with ~ip
#   (failed uninstall leftovers), then retry.
#
# tensorflow-federated 0.33 ships old generated protos; protobuf 4.21+ raises
# "Descriptors cannot be created directly". Pin 3.20.x (still OK for TF 2.13).
#
# numpy / dm-tree / tensorflow-privacy: privacy pins numpy~=1.21 (1.21.x only) while
# dm-tree 0.1.9+ requires numpy>=1.23.3 on Python 3.11. We pin numpy==1.24.3 (TF 2.13),
# dm-tree==0.1.8 (still satisfies TFF ~=0.1.7), and install tensorflow-privacy with --no-deps.
#
# Pip may print "dependency conflicts" with tensorflow-federated after --no-deps install.
# That only means TFF's *declared* pins (TF 2.9, grpcio 1.46, etc.) differ from this stack.
# If "Successfully installed ..." appears, the install worked. Silence noise with:
#   pip install ... --no-warn-conflicts
#
setuptools==70.0.0
wheel
# PyYAML 5.4.x sdist breaks on Python 3.11 + setuptools 70 (build_ext / cython_sources).
PyYAML>=6.0.1,<7
farmhashpy==0.4.0
numpy==1.24.3
pandas>=2.0
scikit-learn>=1.3
grpcio>=1.54.0,<2.0
absl-py>=1.0.0,<2.0.0
attrs>=21.4.0,<22
cachetools>=3.1.1,<3.2
dm-tree==0.1.8
semantic-version>=2.6.0,<3
tqdm>=4.64.0,<5
portpicker>=1.5.2,<2
tensorflow-model-optimization>=0.7.2,<0.8.0
streamlit>=1.33,<2
# tensorflow-privacy (installed with --no-deps) still needs dp_accounting at import time.
dp-accounting==0.4.2
# tensorflow_privacy imports tensorflow_probability (privacy metadata: ~=0.20.0).
tensorflow-probability>=0.20.0,<0.21.0
immutabledict>=2.2,<3
jax
jaxlib
protobuf==3.20.3
tensorflow==2.13.1
#
# After this file installs successfully, run (privacy deps conflict with numpy 3.11 stack):
#   pip install tensorflow-privacy==0.8.10 --no-deps
#   pip install tensorflow-federated==0.33.0 --no-deps